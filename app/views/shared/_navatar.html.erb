<% if current_user %>
  <img src="<%= cl_image_path current_user.photo, height: 30, width: 30, crop: :thumb, gravity: :face if current_user.photo? %>" class="avatar dropdown-toggle" id="navbar-wagon-menu" data-toggle="dropdown">
<% end %>

